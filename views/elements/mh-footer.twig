<footer>
	<div class="row">
		<div class="small-12 medium-6 large-8 columns">
			<div class="mh-media">
				<img src="/img/{{ Site.icon}}" alt="" class="mh-channel-menu-home-icon mh-media-object" width="48">
				<div class="mh-media-body mh-footer-title-block">
					<p class="mh-footer-title">{{ Site.title }}</p>
					<p class="mh-footer-tagline">{{ Site.tagline }}</p>
					<p class="mh-footer-description">{{ Site.description }}</p>
				</div>
			</div>
		</div>
		<div class="small-12 medium-6 large-4 columns">
			<ul class="vcard">
				<li class="fn">{{ Organization.title }}</li>
				<li class="street-address">{{ Organization.address }}</li>
				<li class="locality">{{ Organization.city }}</li>
				<li><span class="state">{{ Organization.state }}</span>, <span class="zip">{{ 'Organization.zip' }}</span></li>
				<li class="phone">986 213 047</li>
				<li class="email"><a href="mailto:{{ Organization.email }}">{{ Organization.email }}</a></li>
			</ul>
		</div>
	</div>
	{# echo $this->Page->block('/menus/wbar', array('bar' => 'main')); #}
	<div class="row">
		{# echo $this->Page->block('/contents/channels/modules/external', array(
			'cache' => '+1 month',
			)); #}

	</div>
	<div class="row">
		<div class="small-12">
			<p class="right">{# echo $this->Html->link(__('Powered by Milhojas. Credits', true), array('plugin' => false, 'controller' => 'pages', 'action' => 'display', 'credits')
		); #}.</p>
		</div>
	</div>
</footer>
