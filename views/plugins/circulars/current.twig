{% extends 'layouts/main.twig' %}
{% block content %}
	<header>
        <h1>Últimas circulares.</h1>
	</header>
    <div class="small-up-1 medium-up-2 row">
        {% for circular in circulars %}
            <div class="column">
                <article class="mh-circular card">
                    <div class="card-divider">
                        <h2>
                            <a href="/circulars/circulars/view/{{ circular.id }}">{{ circular.title }}</a>
                        </h2>

                    </div>
                    <div class="card-section">
                        <p>{{ circular.addressee }}</p>
                        <p>{{ circular.content|nl2br|slice(0, 100) }}…</p>

                    </div>
                    <div class="card-divider">
                        <p>Publicada en {{ circular.pubDate|date('j/n') }}</p>
                        <p>{% if circular.id %}<i class="fi-alert"></i>Require respuesta{% endif %}</p>

                    </div>
                </article>
            </div>

        {% else %}
		    <p>No hay circulares publicadas en este momento.</p>
        {% endfor %}

	</div>
	<footer>{# echo $this->Page->paginator('mh-public-paginator'); #}</footer>
{% endblock %}
