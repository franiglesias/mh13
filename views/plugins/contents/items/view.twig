{% extends 'layouts/story.twig' %}
{% block content %}

	<div class="container mh-channel-menu">{# echo $this->Channel->menu(); #}</div>

	<article class="mh-page">

		<pre>
		{{ dump(item.Item.gallery) }}
		</pre>
		{% if preview %}
			<aside class="mh-preview-ribbon">Así es como se verá</aside>
		{% endif %}
		{# echo $this->Page->isPreview(); #}
		{# echo $this->Page->block('/contents/items/widgets/header', array(
	        'imagePath' => $this->Image->value('path'),
	        'title' => $this->Item->value('title'),
	    )); #}
		{# echo $this->Item->status(); #}
		<div class="row">
			<div class="small-12 medium-7 columns">
				{#
	                if ($this->Images->count() == 1) {
	                    echo $this->Image->render();
	                }
	            #}
				{% if item.Image|length > 1 %}
					{% include ('plugins/images/galleries/lightbox/layout.twig')  %}
				{% endif %}

				{{ item.Item.content|raw }}

				{# if ($showExtras): #}
					{# echo $this->Page->block('/contents/items/widgets/multimedia', array(
	                    'item' => $item,
	                )); #}
				{# endif #}

				{# if ($showExtras): #}
					{# echo $this->Page->block('/contents/items/widgets/downloads', array(
	                    'item' => $item,
	                )); #}
				{# endif #}

				{# echo $this->Comment->render('Contents.Item', $item['Item']['id'], $item['Item']['allow_comments']); #}
			</div>
			<section class="small-12 medium-4 columns">
				{% include ('plugins/contents/items/widgets/abstract.twig') %}
				{% include ('plugins/contents/items/widgets/mdata.twig') %}
				{% include ('plugins/contents/items/widgets/social.twig') %}
				{# echo $this->Page->block('/contents/items/widgets/tags'); #}
				{# echo $this->Page->block('/contents/items/widgets/neighbors', array('neighbors' => $neighbors)); #}
				{# echo $this->Page->block('/contents/channels/blocks/description'); #}
				{# echo $this->Page->block('/contents/items/widgets/license'); #}
			</section>
		</div>
	</article>
{% endblock %}
