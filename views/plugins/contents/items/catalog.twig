{% for article in articles %}
    {% import 'macros/catalog.twig' as catalog %}
    {{ attribute(catalog, layout|default('index'), [article.article, article.blog, article.image.path]) }}
{% endfor %}
